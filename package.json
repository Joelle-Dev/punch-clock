{
  "name": "punch-clock",
  "version": "1.0.0",
  "description": "轻量级打卡记录应用",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p dist && touch dist/.nojekyll",
    "build:docs": "vite build && touch dist/.nojekyll && rm -rf docs && cp -r dist docs && touch docs/.nojekyll",
    "preview": "vite preview"
  },
  "keywords": ["punch", "clock", "attendance", "pwa"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.17.0"
  }
}

